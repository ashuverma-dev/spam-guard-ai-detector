# Editable source code

This folder contains the editable Next.js source for SpamShield. The repository root contains the static GitHub Pages build currently used by the live site.

To work on the source locally:

```bash
cd source
npm install
npm run dev
```

The detection runs entirely in the browser; no backend or API key is required.
